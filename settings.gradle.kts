pluginManagement {
    repositories {
        maven { url = uri("https://nxrm.dst.tk-inline.net/repository/maven-public/") }
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "dpop-demo"