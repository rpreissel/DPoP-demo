package com.example.dpop.tool_spi

/** Factor kind a method covers; self-reported by the module (docs/03-tool-architektur.md). */
enum class FactorType {
    KNOWLEDGE,
    POSSESSION,
    INHERENCE
}
