package com.example.dpop.id_fsc

import com.example.dpop.tool_spi.MethodFamily

/** No sibling today (ident-fsc is the only tool for "fsc") - kept as its own file for the same uniform shape every other module follows. */
internal val FSC_METHOD = MethodFamily("fsc")
