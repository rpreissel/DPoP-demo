# Project Instructions for AI Agents

This file is intentionally short to reduce repeated context tokens.

## Primary instruction source

- Use `AGENTS.md` as the canonical project instruction file.
- Start each session with `docs/00-agent-quickstart.md` before opening deep docs.

## Practical defaults

- Use `bd` (beads) for task tracking (`bd prime` for workflow details).
- Do not push or run remote sync operations without explicit instruction.
- Keep changes small, targeted, and aligned with documented architecture.
