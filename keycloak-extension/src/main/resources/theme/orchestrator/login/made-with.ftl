<#-- Which theme drew this page - shown in the dark band at the bottom (orchestrator.css,
     .orchestrator-made-with). The Keycloakify theme says the same in its Layout.tsx. -->
<#macro note>
    <p class="orchestrator-made-with">${t.of("Erstellt mit {technik}", {"technik": "FreeMarker"})}</p>
</#macro>
